# Backend API base URL (used in production; dev uses Vite proxy to /api)
# Production (GCP Cloud Run): https://YOUR_SERVICE-xxx.run.app/api
# Local dev: leave unset to use /api (proxied by vite.config.ts to http://localhost:5000)
# Backend must run on port 5000 for the proxy to work. For E2E without proxy, set:
# VITE_API_BASE_URL=http://localhost:5000/api
